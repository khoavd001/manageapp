<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manage">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Manage">
        <activity android:name=".Activity.ui.song.ChooseSingerActivity"></activity>
        <activity android:name=".Adapter.SingerSpinnerAdapter" />
        <activity android:name=".Adapter.SingerSpinnerActivity" />
        <activity android:name=".Activity.ui.singer.DetailSinger" />
        <activity android:name=".Activity.ui.song.UpdateSong" />
        <activity
            android:name=".Activity.Manage"
            android:label="@string/title_activity_manage"
            android:theme="@style/Theme.Manage.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>